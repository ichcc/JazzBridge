<svg width="1200" height="630" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="1200" height="630" fill="#1a1a1a"/>

  <!-- Gradient overlay -->
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#2a2a2a;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#1a1a1a;stop-opacity:1" />
    </linearGradient>
  </defs>
  <rect width="1200" height="630" fill="url(#grad)"/>

  <!-- Main icon -->
  <text x="600" y="220" font-size="140" text-anchor="middle" fill="#fff">🎷</text>

  <!-- Title -->
  <text x="600" y="340" font-size="48" font-weight="bold" text-anchor="middle" fill="#fff" font-family="system-ui, -apple-system, sans-serif">
    Latest Jazz Albums
  </text>

  <!-- Subtitle -->
  <text x="600" y="390" font-size="32" text-anchor="middle" fill="#888" font-family="system-ui, -apple-system, sans-serif">
    from All About Jazz
  </text>

  <!-- Info -->
  <text x="600" y="480" font-size="24" text-anchor="middle" fill="#666" font-family="system-ui, -apple-system, sans-serif">
    Daily updated • Universal streaming links
  </text>

  <!-- URL -->
  <text x="600" y="560" font-size="20" text-anchor="middle" fill="#555" font-family="monospace">
    jazzbridge.pages.dev
  </text>
</svg>
